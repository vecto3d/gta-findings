# Towing

**Source:** `towing.c` (324K)

Scaleform `MIDSIZED_MESSAGE` only — the hook mechanics are native calls, not UI.

Anim dict `oddjobs@towingcome_here`, gestures via `gestures@m@standing@casual`.
Relationship group `TOWBUDDIES`.

Vehicles `towtruck`, `towtruck2`. Note `SET_VEHICLE_DISABLE_TOWING` has 13475
call sites repo-wide, so R* actively suppresses towing in most other scripts.
